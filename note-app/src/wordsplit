def get_initials(name):
    words = name.split()
    initials = ''.join(word[0].upper() for word in words)
    return initials

# Example
full_name = "My Name"
result = get_initials(full_name)
print(result)